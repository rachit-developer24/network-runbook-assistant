spring:
  application:
    name: network-runbook-assistant

  config:
    import: optional:file:.env[.properties]

  datasource:
    url: jdbc:postgresql://localhost:5432/runbookdb
    username: runbook
    password: ${POSTGRES_PASSWORD}

  ai:
    vectorstore:
      pgvector:
        initialize-schema: false
